meta {
  name: validate rock
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/games
  body: json
  auth: inherit
}

body:json {
  { "playerChoice": "stein" }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
